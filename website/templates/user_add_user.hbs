{{#*inline "page"}}

<!-- 
Users Add new user -->
<section class="section">
    <div class="container">
        <div class="columns is-mobile">
            <div class="column">
                <p class="is-size-3">Users</p>
                <p class="subtitle">Add new user</p>
            </div>
        </div>
        <div class="field">
            <label class="label">Username</label>
            <div class="control">
                <input class="input" type="text" placeholder="e.g johnsmith">
            </div>
        </div>
        <div class="field">
            <label class="label">Email</label>
            <div class="control">
                <input class="input" type="email" placeholder="e.g. alexsmith@gmail.com">
            </div>
        </div>
        <div class="field">
            <label class="label">Name</label>
            <div class="control">
                <input class="input" type="text" placeholder="e.g John Smith">
            </div>
        </div>
        <div class="field">
            <div class="button-group">
                <button class="button is-info is-outlined">Add user</button>
                <button class="button">Cancel</button>
            </div>
        </div>
    </div>
</section>

{{/inline}}
{{~> (parent)~}}